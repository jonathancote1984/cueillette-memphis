# Critique design — Cueillette Québec édition Memphis (29e passe)

Method: dual-agent (A: critique design · B: détecteur + navigateur) — sous protocole APEX (run 20260816-172409-cf2fead4)

## Verdict de spécificité

**~90 %/90 %** — 0 ombre floutée, 0 uppercase, 0 boîte système, 0 hex hors palette, 0 font-weight hors jeu, 0 opacité texte < .8 (batterie Python 0 trouvaille).

## Design Health Score : **38,5/40 — plateau honnête** (H4 +0,5 confirmation rationnelle ; H1 −0,5 faux badge possible)

## Problèmes corrigés (chantier 29e, commit `f84c9be`, v57)

- **P1-1 (SÉCURITÉ)** : le filet retourné — aucune unicité sur `e-nom` + badge des cueillettes résolu par nom avec le guide en premier → une variété perso MORTELLE homonyme d'un comestible du guide affichait ✅ Comestible sur ses cueillettes. **Contrôle de collision à l'enregistrement** (confirmation si homonyme) + **résolution par `especeId` D'ABORD** (`BASE_COMPLETE().find(x => x.id === c.especeId)`), nom en fallback.
- **P3-1** : la confirmation promettait la perte de la checklist, jamais exécutée → `Stockage.effacer('checklist', id)`.
- **P3-2** : puces masquées sans distinction → classe `.puce.retiree` (pointillés, opacité .55, tooltip).
- **P3-3** : `supprimees` non normalisé à l'import → normalisation (objets hérités ou ids nus).
- **P3-4** : DESIGN.md drift → btn-petit (10px/14px, min-height 44px) + scale(.96) documentés.
- **P3-5** : feuille idphoto rouverte d'office après Escape → fermer pendant l'analyse = `annulerAnalyse()`.

## Assessment B (29e — RÉUSSI)

- **101 findings, 0 drift** ; **runtime 20/20 vert** ; 5/5 fixes `1598f06` vérifiés (round-trip tombes export→wipe→import). Incident popup Chrome résolu avec un **Chrome headless piloté par CDP** (fallback sans consentement) — technique documentée dans le skill `frontend-mechanical-assessment`.

## Tendances

- **Score : 29 → … → 38,5 → 38,5 (29e)** — plateau. Le filet ne peut plus se retourner : l'identité exacte (especeId) prime sur le nom, et l'homonymie est interceptée à la création.
